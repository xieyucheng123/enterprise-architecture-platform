name: Monthly Cleanup

on:
  schedule:
    - cron: '0 3 1 * *'
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  cleanup:
    uses: link-seek/issue-resolver/.github/workflows/monthly-cleanup.yml@main
    secrets: inherit
